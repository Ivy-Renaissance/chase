#include <stdio.h>
#include <math.h>
int main()
{
	double a,b,c,s,area;
	printf("请输入三角形的三个边长："); 
	scanf("%if%if%if,&a,&b,&c");
	s=(a+b+c)/2;
	area=sqrt(s*(s-a)*(s-b)*(s-c));
	printf("the area of the triangle is:%f\n",area);
	
	return 0
	 
	
}
